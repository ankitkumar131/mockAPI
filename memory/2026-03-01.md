# 2026-03-01

## CloudCanvas-TF — Major Overhaul (12:35 PM – 1:10 PM)

**Project:** `C:\Users\Admin\Desktop\CloudCanvas-TF` — Angular app that visually designs GCP infrastructure and generates Terraform code. Uses a plugin-based architecture (`ResourcePlugin` interface).

### GCP Services Expansion
- Expanded from **12 plugins → 66 plugins** covering all major GCP services
- Restructured `src/app/infra/plugins/` from flat directory into **13 categorized subdirectories**: compute, network, load-balancing, storage, kubernetes, database, serverless, messaging, analytics, ai-ml, security, devops, monitoring
- Updated `infra-graph.model.ts` with 62 `ResourceKind` types, expanded `EdgeRelationship` to 17 values, added `RESOURCE_CATEGORIES` and `RESOURCE_ICONS` maps
- Updated `plugin-registry.service.ts` with all imports/registrations
- Updated `constants.ts` and `palette.component.ts` for new category ordering
- Used 5 parallel sub-agents for plugin creation, 1 for build error fixes

### UI Overhaul (eraser.io-inspired)
- Overhauled all 7 SCSS files for smoother, more modern feel
- Added spring animation curves, glassmorphism (backdrop-filter blur), subtle gradient backgrounds
- Refined node hover/selection glows, edge interactions, floating zoom controls
- Toggle switches with spring knob, slide-in diagnostics, expand-on-focus search
- Final build: **92.66 KB** gzipped, zero errors

### Key files modified
- `src/app/core/models/infra-graph.model.ts` — types + categories + icons
- `src/app/infra/plugin-registry.service.ts` — all 66 plugin registrations
- `src/app/shared/constants.ts` — category order
- `src/styles.scss` + all component `.scss` files — UI overhaul
- 54 new plugin `.ts` files across 13 subdirectories

## CloudCanvas-TF — UI Polish & Bug Fixes (1:10 PM – 1:25 PM)

### Eraser.io-Inspired UI Overhaul
- Rewrote all 7 SCSS files (styles.scss, app.component, top-bar, canvas, palette, inspector, code-preview, diagnostics)
- Added spring animation curves, glassmorphism (backdrop-filter blur), subtle radial gradient backgrounds
- Node hover/selection glows, smooth edge interactions, floating zoom controls with backdrop blur
- Toggle switches with spring knob animation, slide-in diagnostics, expand-on-focus search
- Final build: **92.68 KB** gzipped

### Bug Fixes
1. **Dropdown text invisible** — Added explicit `option { background; color }` inside `.inspector__select` for dark theme
2. **Palette categories open by default** — Changed `expandedCategories` to empty `Set()` so all categories start collapsed
3. **Hardcoded Terraform references** — Fixed 5 plugins to resolve edge references dynamically via `ctx.getNodeReference`:
   - `sql-database` → resolves `google_sql_database_instance`
   - `spanner-database` → resolves `google_spanner_instance`
   - `container-node-pool` → resolves `google_container_cluster`
   - `kms-crypto-key` → resolves `google_kms_key_ring`
   - `bigquery-table` → resolves `google_bigquery_dataset`
